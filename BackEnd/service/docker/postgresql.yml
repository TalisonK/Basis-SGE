version: '2'
services:
  sge-postgresql:
    image: postgres:11.5
    # volumes:
    #     - ~/volumes/jhipster/srsservice/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sge
      - POSTGRES_PASSWORD=sge
    ports:
      - 5432:5432
